# dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
.next/
out/

# wrangler / cloudflare
.wrangler/
packages/kyc-server/.wrangler/

# production builds
build/
dist/

# env & secrets — never commit these
.env
.env.*
!.env
.dev.vars
.env.local
.mcp.json

# lock files (monorepo uses npm workspaces)
pnpm-lock.yaml
pnpm-workspace.yaml
package-lock.json

# misc
.DS_Store
Thumbs.db
*.tmp
*.swp
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts
# (next-env.d.ts covered above)
.next/*

# tools & editor
.vscode/*
.idea/*
.history/*
.claude/*
.specify/*
.github/*
!.github/copilot-instructions.md
!.github/copilot-review-instructions.md

# vercel
.vercel

# project-specific
certificates/
specs/
docs/*
spec.md
CLAUDE.md
debug.log

# OpenNext (Cloudflare Workers build output)
.open-next

# Security posture doc — keep this one tracked despite docs/* ignore
!docs/security-posture.md

# Vendored browser runtimes for ID capture (~43 MB) — regenerated by
# `npm run sync:vendor`, which also runs on postinstall. Not committed here
# because ID capture is moving to the ramaaz-kyc repo and git history is forever.
public/vendor/
