include=optional
# @cloudflare/next-on-pages peer-wants @cloudflare/workers-types ^4 while
# wrangler ^4.71 peer-wants ^5. These are optional type/CLI tooling peers whose
# version skew doesn't affect the build, so don't let the mismatch fail
# `npm install` / `npx @cloudflare/next-on-pages` in CI.
legacy-peer-deps=true
