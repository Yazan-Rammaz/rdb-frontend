# Committed defaults for all environments (override in .env.local / CI env).
#
# Deliberately empty. It previously carried NEXT_PUBLIC_USE_SERVER_ACTIONS (the
# injected-actions layer is gone) and NEXT_PUBLIC_OPAQUE_API (the opcode gateway
# is gone). Both were read by nothing.
